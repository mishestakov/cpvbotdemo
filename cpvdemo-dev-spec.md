# CPV Demo: Developer Spec (из прототипа)

## 1) Scope
Этот документ фиксирует, как сейчас работает прототип `cpvdemo`:
- сущности и JSON-хранилище,
- режимы публикации и статусы офферов,
- тексты сообщений бота,
- API-контракты,
- артефакты, которые стоит включить в ТЗ.

Источник истины по Telegram API: `telegrambotapi.md`.

## 2) SQL-структура данных (target schema)
Текущий прототип хранит данные в `cpvdemo/data/db.json`. Ниже SQL-модель (PostgreSQL), эквивалентная текущей логике.

```sql
-- enums
CREATE TYPE posting_mode AS ENUM (
  'auto',               -- тихий автопост без карточки оффера в личке
  'auto_with_precheck', -- карточка предпросмотра; если не отклонить до дедлайна, пост будет опубликован
  'manual_approval',    -- публикация только после явного подтверждения
  'manual_posting'      -- блогер получает ERID и публикует вручную
);

CREATE TYPE offer_status AS ENUM (
  'pending_precheck',          -- ждём решение в режиме предпросмотра
  'pending_approval',          -- ждём подтверждение в режиме manual_approval
  'pending_manual_posting',    -- ждём действие блогера в ручном режиме (ERID/перенос/отказ)
  'manual_waiting_publication',-- ERID выдан, ожидается пост в канале
  'manual_queued_publication', -- блогер подтвердил, что поставил пост в отложку/очередь
  'manual_publication_found',  -- пост с ERID найден, идёт hold-период до начисления
  'scheduled',                 -- оффер согласован, ждёт времени публикации
  'rewarded',                  -- публикация зачтена, вознаграждение начислено
  'archived_not_published',    -- публикация не состоялась в допустимом окне
  'auto_publish_error',        -- попытка автопоста завершилась ошибкой
  'declined_by_blogger',       -- блогер отклонил оффер на этапе выбора действия (pending_*)
  'cancelled_by_advertiser',   -- рекламодатель отменил оффер
  'cancelled_by_blogger'       -- блогер отменил уже активный/согласованный оффер (scheduled/manual_waiting/...)
);

CREATE TYPE auth_session_status AS ENUM (
  'pending_start',   -- сессия создана в вебе, ждём /start в Telegram
  'awaiting_channel',-- /start получен, ждём выбор канала (request_chat/chat_shared)
  'connected',       -- канал выбран, веб-сессия связана с блогером
  'expired'          -- сессия протухла по TTL
);

-- bloggers
CREATE TABLE bloggers (
  id                   BIGINT PRIMARY KEY,         -- внутренний id блогера (равен tg user id)
  tg_user_id           BIGINT NOT NULL UNIQUE,     -- Telegram user id
  tg_username          TEXT,                       -- username пользователя в Telegram
  chat_id              BIGINT,                     -- id личного чата с ботом
  channel_id           BIGINT,                     -- выбранный по умолчанию канал блогера
  connected_at         TIMESTAMPTZ NOT NULL DEFAULT now() -- время первой успешной связки веб+бот
);

-- channels
CREATE TABLE channels (
  id                   BIGSERIAL PRIMARY KEY,      -- внутренний id канала
  blogger_id           BIGINT NOT NULL REFERENCES bloggers(id) ON DELETE CASCADE, -- владелец канала
  chat_id              BIGINT NOT NULL UNIQUE,     -- Telegram channel id
  title                TEXT NOT NULL DEFAULT '',   -- title канала
  username             TEXT NOT NULL DEFAULT '',   -- @username канала (если есть)
  posting_mode         posting_mode NOT NULL DEFAULT 'auto_with_precheck', -- текущий режим размещения
  weekly_post_limit    INT NOT NULL DEFAULT 21 CHECK (weekly_post_limit BETWEEN 1 AND 28), -- недельный лимит активных офферов
  bot_connected        BOOLEAN NOT NULL DEFAULT FALSE, -- бот присутствует в канале с правами публикации
  bot_member_status    TEXT NOT NULL DEFAULT 'unknown', -- статус бота в канале из Telegram API
  auto_paused_until_at TIMESTAMPTZ,                -- до какого времени активна пауза автопубликации
  auto_pause_message_id BIGINT,                    -- id сообщения бота, через которое включили паузу
  created_at           TIMESTAMPTZ NOT NULL DEFAULT now(), -- создание канала в системе
  updated_at           TIMESTAMPTZ NOT NULL DEFAULT now()  -- последнее обновление настроек канала
);

-- weekly schedule slots for channel
CREATE TABLE channel_schedule_slots (
  channel_id           BIGINT NOT NULL REFERENCES channels(id) ON DELETE CASCADE, -- канал, к которому относится слот
  day_of_week          INT NOT NULL CHECK (day_of_week BETWEEN 1 AND 7),   -- день недели: 1=Mon ... 7=Sun
  hour_of_day          INT NOT NULL CHECK (hour_of_day BETWEEN 0 AND 23),  -- час публикации (целый час)
  PRIMARY KEY (channel_id, day_of_week, hour_of_day)
);

-- offers
CREATE TABLE offers (
  id                        BIGINT PRIMARY KEY,   -- id оффера (в прототипе стартует с 1001)
  blogger_id                BIGINT NOT NULL REFERENCES bloggers(id) ON DELETE RESTRICT, -- назначенный блогер
  channel_id                BIGINT NOT NULL REFERENCES channels(id) ON DELETE RESTRICT, -- назначенный канал
  chat_id                   BIGINT NOT NULL,      -- id личного чата блогера (куда слать карточки)
  created_at                TIMESTAMPTZ NOT NULL DEFAULT now(), -- момент создания оффера
  scheduled_at              TIMESTAMPTZ NOT NULL, -- выбранное время публикации
  availability_from_at      TIMESTAMPTZ NOT NULL, -- начало окна допустимого размещения
  availability_to_at        TIMESTAMPTZ NOT NULL, -- конец окна допустимого размещения
  mode_at_creation          posting_mode NOT NULL, -- режим канала в момент создания оффера
  status                    offer_status NOT NULL, -- текущий статус state machine
  cpv                       INT NOT NULL CHECK (cpv >= 100), -- ставка CPV/CPM из оффера
  estimated_income          INT NOT NULL,         -- прогноз дохода блогера (обычно cpv*0.85)
  text_raw                  TEXT NOT NULL,        -- рекламный текст без ERID
  text_marked               TEXT NOT NULL,        -- рекламный текст с ERID-маркером
  erid_tag                  TEXT NOT NULL,        -- erid marker (например demo-1001)
  decision_deadline_at      TIMESTAMPTZ,          -- дедлайн решения для pending_precheck
  ui_state                  TEXT NOT NULL DEFAULT 'main', -- UI карточки: main | pick_time
  selected_date_page        INT NOT NULL DEFAULT 0, -- текущая страница дат в picker-е
  message_id                BIGINT,               -- id служебной карточки оффера в личке
  ad_message_id             BIGINT,               -- id сообщения с исходным рекламным текстом
  channel_post_id           BIGINT,               -- id поста в канале (если найден/опубликован)
  manual_publication_found_at TIMESTAMPTZ,        -- когда найден post с ERID в канале
  manual_reminder_count     INT NOT NULL DEFAULT 0, -- сколько reminder отправлено по ручной ветке
  manual_reminder_sent_at   TIMESTAMPTZ,          -- время последнего reminder
  blogger_decline_reason    TEXT                  -- причина отклонения блогером (если собирается)
);

-- auth sessions
CREATE TABLE auth_sessions (
  token                 TEXT PRIMARY KEY,         -- токен deep-link /start
  created_at            TIMESTAMPTZ NOT NULL DEFAULT now(), -- когда создана auth-сессия
  expires_at            TIMESTAMPTZ NOT NULL,     -- ttl auth-сессии
  status                auth_session_status NOT NULL, -- этап auth-процесса
  tg_user_id            BIGINT,                   -- telegram user id после /start
  tg_username           TEXT,                     -- telegram username после /start
  blogger_id            BIGINT REFERENCES bloggers(id) ON DELETE SET NULL, -- связанный блогер
  connected_at          TIMESTAMPTZ,              -- когда сессия стала connected
  error                 TEXT                      -- текст ошибки/причины (если есть)
);

-- indexes
CREATE INDEX idx_offers_status_scheduled_at ON offers(status, scheduled_at);
CREATE INDEX idx_offers_blogger_id_status ON offers(blogger_id, status);
CREATE INDEX idx_offers_channel_id_status ON offers(channel_id, status);
CREATE INDEX idx_channels_blogger_id ON channels(blogger_id);
CREATE INDEX idx_auth_sessions_status_expires_at ON auth_sessions(status, expires_at);

```

## 3) Режимы публикации
- `auto` — тихий автопост, оффер в личку не отправляется.
- `auto_with_precheck` — присылается предпросмотр; если не отклонить до дедлайна, уходит в публикацию.
- `manual_approval` — публикация только после явного подтверждения.
- `manual_posting` — блогер берёт ERID и публикует вручную.

## 4) Статусы оффера
Рабочие статусы:
- `pending_precheck`
- `pending_approval`
- `pending_manual_posting`
- `manual_waiting_publication`
- `manual_queued_publication`
- `manual_publication_found`
- `scheduled`
- `rewarded`
- `archived_not_published`
- `auto_publish_error`
- `declined_by_blogger`
- `cancelled_by_advertiser`
- `cancelled_by_blogger`

Уточнение по похожим статусам:
- `declined_by_blogger` — отказ блогера в точке принятия решения по новому офферу (кнопка `Отклонить` в `pending_precheck` / `pending_approval` / `pending_manual_posting`).
- `cancelled_by_blogger` — отмена блогером уже активного оффера после предварительного принятия (например через `🚫 Не публиковать` / `🚫 Отказаться`).

Диаграммы состояния/сообщений:
- `cpvdemo-offer-state-machine.mmd`
- `cpvdemo-message-flow.mmd`

## 5) Процесс под капотом (детально)
Ниже не диаграмма, а последовательность того, что реально делает backend в `cpvdemo/server.js`.

### 5.1 Источники событий
Система обрабатывает 3 типа событий:
1. Входящие Telegram webhook update (`/api/telegram/webhook`):
   - `message` (`/start`, `/mode`, `/pause`, `chat_shared`),
   - `callback_query` (inline-кнопки офферов/режима/паузы),
   - `channel_post` и `edited_channel_post` (детект ручной публикации по ERID).
2. HTTP API вызовы веба:
   - auth session create/get,
   - advertiser offer create/cancel,
   - blogger cancel/mode/settings.
3. Фоновый таймер (`OFFER_DEADLINE_CHECK_INTERVAL_MS`):
   - `processOfferDeadlines()`,
   - `processAutoPauseExpirations()`.

### 5.2 Boot и периодические задачи
При старте процесса:
1. Поднимается HTTP server.
2. Поднимается bot webhook через `setWebhook` (если есть `WEBHOOK_BASE_URL` и `WEBHOOK_SECRET_TOKEN`).
3. Один раз выполняются `processOfferDeadlines()` и `processAutoPauseExpirations()`.
4. Ставятся два интервала:
   - reconnect bot (`BOT_CONNECT_RETRY_INTERVAL_MS`) при ошибках Telegram API;
   - offer tick (`OFFER_DEADLINE_CHECK_INTERVAL_MS`).

### 5.3 Авторизация блогера (веб -> бот -> выбор канала)
1. Веб вызывает `POST /api/auth/session`.
2. Backend создаёт токен, кладёт `authSessions[token]` со статусом `pending_start`, TTL = `AUTH_SESSION_TTL_MS`.
3. Пользователь открывает deep-link `https://t.me/<bot>?start=<token>`.
4. На `/start <token>` backend:
   - проверяет токен и неистёкшую сессию;
   - upsert-ит блогера;
   - переводит сессию в `awaiting_channel`;
   - отправляет клавиатуру `request_chat`.
5. На `chat_shared` backend:
   - берёт последнюю сессию `awaiting_channel` для пользователя;
   - защищается от устаревших `chat_shared` (по timestamp);
   - создаёт/обновляет канал;
   - подтягивает состояние бота в канале (`hydrateChannelFromTelegram`);
   - переводит сессии блогера в `connected`;
   - шлёт один из двух исходов:
     - бот не подключён/без прав: просьба добавить бота в канал;
     - бот подключён: настройка завершена.

### 5.4 Создание оффера рекламодателем
Вход: `POST /api/advertiser/offers` с `dateFrom`, `dateTo`, `cpv`, `text`, списком каналов или блогеров.

Порядок:
1. Валидация диапазона дат и таргетов.
2. Для каждого таргета выбирается канал и блогер.
3. Канал пропускается, если:
   - режим поддерживает паузу и сейчас `autoPausedUntilAt > now`,
   - активных офферов уже >= `weeklyPostLimit`,
   - в диапазоне нет доступных слотов.
4. Для прошедших каналов выбирается слот (`pickScheduledTimeForBlogger`) с учётом:
   - недельного расписания канала,
   - уже занятых активных слотов блогера.
5. Создаётся оффер:
   - `modeAtCreation` фиксируется в момент создания,
   - стартовый `status`:
     - `auto` -> `scheduled`,
     - `auto_with_precheck` -> `pending_precheck`,
     - `manual_approval` -> `pending_approval`,
     - `manual_posting` -> `pending_manual_posting`.
6. Уведомление блогеру:
   - для `auto` не отправляется (тихий режим),
   - для остальных через `upsertOfferMessage`.

### 5.5 Как работает карточка оффера в боте
`upsertOfferMessage()` делает две сущности:
1. Отдельное сообщение с `textRaw` (если ещё нет `adMessageId`).
2. Служебная карточка-ответ (`messageId`) с summary + inline-кнопками.

Важно:
- `mode=auto`: карточка вообще не отправляется.
- При повторных изменениях оффера бот старается редактировать существующую карточку, а не слать новую.

### 5.6 Callback-действия (inline-кнопки)
Маршрутизация по `of:<action>:<offerId>[:arg]`.

Основные действия:
1. `ap`:
   - допустим только в `pending_precheck|pending_approval`,
   - переводит в `scheduled`, шлёт `Оффер #... подтверждён.`
2. `dr`:
   - допустим только в `pending_*`,
   - переводит в `declined_by_blogger`.
3. `bc`:
   - допустим для active-статусов,
   - переводит в `cancelled_by_blogger`.
4. `tm/pd/ps/tb`:
   - включает picker времени (`uiState=pick_time`),
   - выбирает слот и делает `reschedule`,
   - возвращает в `uiState=main`.
5. `me` (ручной режим):
   - `pending_manual_posting -> manual_waiting_publication`,
   - отправляет ERID + `textMarked`.
6. `mq` (ручной режим):
   - `manual_waiting_publication -> manual_queued_publication`,
   - фиксирует, что блогер поставил публикацию в отложку/очередь.

Устаревшие callback-и `rb/rr` в коде оставлены только как заглушка: `"Шаг больше не используется"`.

### 5.7 Фоновая state machine (строгий порядок внутри tick)
В `processOfferDeadlines()` проверки идут сверху вниз. Это важно, т.к. порядок влияет на итоговый статус.

Порядок:
1. `pending_precheck` + истёк `decisionDeadlineAt` -> `approveOffer()` -> `scheduled`.
2. `pending_approval` + наступил `scheduledAt` -> `archived_not_published`.
3. `pending_manual_posting` + наступил `scheduledAt`:
   - если `now > availabilityToAt` -> `archived_not_published`,
   - если нет будущих слотов -> `archived_not_published`,
   - иначе reminder (до `MANUAL_PENDING_REMINDER_MAX`, интервал `MANUAL_PENDING_REMINDER_INTERVAL_MS`).
4. `manual_waiting_publication` или `manual_queued_publication` + наступил `scheduledAt` -> `archived_not_published`.
5. `scheduled` + наступил `scheduledAt`:
   - для `auto|auto_with_precheck|manual_approval`:
     - успешный `sendMessage` в канал -> `rewarded`,
     - иначе `auto_publish_error`;
   - для `manual_posting`: `archived_not_published`.
6. `manual_publication_found` + прошёл hold (`MANUAL_PUBLICATION_HOLD_MS`) -> `rewarded`.

### 5.8 Детект ручной публикации в канале
На `channel_post/edited_channel_post`:
1. По `chat_id` находится канал.
2. Из текста/caption берётся body.
3. Ищутся офферы канала в `manual_waiting_publication|manual_queued_publication`.
4. Если body содержит `eridTag`, оффер -> `manual_publication_found`, фиксируется `manualPublicationFoundAt`.
5. Блогеру отправляется сообщение про hold перед начислением.

### 5.9 Пауза автопубликаций
Включение паузы:
1. Через `/pause` panel + callback `pause:set:<channelId>:24h`.
2. Разрешено только в режимах `auto|auto_with_precheck|manual_approval`.
3. Сохраняется `autoPausedUntilAt = now + AUTO_PAUSE_DURATION_MS`.

Снятие паузы:
1. Ручное: callback `resume`.
2. Автоматическое: `processAutoPauseExpirations()` при `autoPausedUntilAt <= now`.
3. На авто-снятии бот шлёт reply: `Пауза завершена, публикации снова активны.`

Эффект паузы:
- при `POST /api/advertiser/offers` канал с активной паузой не получает новый оффер (`skipped` с причиной).

### 5.10 Отмена оффера через API
1. `POST /api/offers/cancel` (блогерская отмена по token):
   - проверка владельца оффера,
   - `status -> cancelled_by_blogger`.
2. `POST /api/advertiser/offers/cancel`:
   - `status -> cancelled_by_advertiser`,
   - блогеру отправляется уведомление (кроме тихого `auto`).

### 5.11 Идемпотентность и защитные проверки
1. Повторное нажатие кнопки в неподходящем статусе не ломает процесс:
   - callback вернёт `Оффер уже в статусе: ...`.
2. Callback из чужого чата отклоняется (`Этот оффер не для вас`).
3. Если webhook secret не совпал, update отбрасывается с `403`.
4. Test API (`/api/test/*`) доступен только при `ALLOW_TEST_API=true` и только с localhost.

### 5.12 Примечание по диаграммам в репозитории
1. `cpvdemo-offer-state-machine.mmd` — целевая state machine (актуализируется под код).
2. `cpvdemo-message-flow.mmd` — полезен как обзор UX, но содержит исторические элементы; для ТЗ приоритет у фактического поведения из `server.js`.

## 6) UX-сообщения (каталог)

### 6.1 Авторизация и выбор канала
- Успех start: `"Авторизация успешна. Теперь выберите канал, где вы админ:"`
- Ошибка кнопки выбора: `"Не удалось отправить кнопку выбора канала. Отправьте /start еще раз."`
- Канал выбран, бот не добавлен:
  - `Канал подтверждён.`
  - `Добавьте бота @... в канал как администратора с правом публикации сообщений.`
  - `После этого возвращайтесь в личный кабинет и настройте график публикаций.`
- Канал выбран, бот уже подключен:
  - `"Отлично, бот уже подключен к каналу. Настройка завершена, можете возвращаться в личный кабинет."`

### 6.2 Карточки оффера
`pending_precheck`:
- `В вашем канале запланирован пост с оплатой за просмотры...`
- Кнопки: `✅ Подтвердить`, `🕒 Выбрать другое время`, `❌ Отклонить`.

`pending_approval`:
- `Подтвердите размещение поста...`
- `Подтвердите пост, чтобы не упустить свой доход!`
- Кнопки как выше.

`pending_manual_posting`:
- `Предложение публикации...`
- `Если вы хотите взять это размещение в работу, нажмите «Получить ERID».`
- Кнопки: `🏷 Получить ERID`, `🕒 Выбрать другое время`, `❌ Отклонить`.

`manual_waiting_publication`:
- `Ожидаем публикацию... ERID: ...`
- Кнопки: `✅ Добавил в очередь постинга`, `🚫 Не публиковать`.

`manual_queued_publication`:
- `Пост добавлен в очередь публикации... Спасибо, ждём выхода публикации.`
- Кнопка: `🚫 Не публиковать`.

`manual_publication_found`:
- `Публикация ... найдена`
- `Ожидаем <N мин/сек> до начисления.`

### 6.3 Служебные уведомления
- Авто-подтверждение precheck: `Оффер #... автоматически подтверждён...`
- Подтверждение вручную: `Оффер #... подтверждён.`
- Автопост успешен: `Оффер #... опубликован автоматически в канале.`
- Автопост ошибка: `... автопубликация не выполнена ... перенесена в архив.`
- Рекламодатель отменил: `Рекламодатель отменил оффер #.... Публикация не состоится.`
- Блогер отменил: `Оффер #... отменён блогером.`
- ERID выдан: `ERID для оффера #...` + `Публикация будет засчитана...`
- Публикация найдена: `Публикация найдена. Она должна провисеть минимум ...`
- Начисление: `Деньги за публикацию начислены, теперь её можно удалить.`

### 6.4 /mode и /pause
- `/mode`: панель с выбранным каналом, текущим режимом, пояснениями каждого режима и статусами каналов.
- `/pause`: панель паузы только для auto/precheck/approval:
  - активна: `⏸ Пауза установлена до ...` + кнопка `▶️ Возобновить`;
  - неактивна: `Автопубликации активны...` + кнопка `⏸ Пауза 24 часа`.

## 7) API-контракты (backend)

Публичные snapshot API:
- `GET /api/state?token=...`
- `GET /api/advertiser/state`
- `GET /api/admin/state`

Auth:
- `POST /api/auth/session`
- `GET /api/auth/session?token=...`

Blogger/channel:
- `POST /api/channel/mode`
- `POST /api/channel/settings`
- `POST /api/offers/cancel`

Advertiser:
- `POST /api/advertiser/offers`
- `POST /api/advertiser/offers/cancel`

Test-only (localhost + `ALLOW_TEST_API=true`):
- `POST /api/test/tick`
- `POST /api/test/offers`

Telegram webhook:
- `POST /api/telegram/webhook` (обязательный header `x-telegram-bot-api-secret-token`)

## 8) ENV-параметры, влияющие на поведение
Ключевые:
- `BOT_TOKEN`
- `WEBHOOK_BASE_URL`
- `WEBHOOK_SECRET_TOKEN`
- `ALLOW_TEST_API`
- `PRECHECK_DECISION_MS`
- `OFFER_DEADLINE_CHECK_INTERVAL_MS`
- `MANUAL_PUBLICATION_HOLD_MS`
- `MANUAL_PENDING_REMINDER_MAX`
- `MANUAL_PENDING_REMINDER_INTERVAL_MS`
- `AUTO_PAUSE_DURATION_MS`

## 9) Что еще полезно отправить в ТЗ (помимо этого документа)
1. Критерии приёмки по каждой ветке статусов (Given/When/Then, 1-2 шага на ветку).
2. Таблица прав Telegram-бота в канале (минимальные permissions + что ломается без них).
3. SLA/тайминги прототипа (сколько ждём действие блогера, как часто reminder, когда архивируем).
4. Негативные кейсы UX (бот не в канале, таймауты Telegram API, устаревшая auth-сессия).
5. Что не входит в scope (например, полноценный DM/CRM, billing, антифрод, аналитика post-view).
6. План миграции из JSON на production-хранилище (если прототип пойдёт дальше).

## 10) Минимальный one-shot prompt для повторения прототипа
Собери Node.js демо-сервис CPV-планировщика Telegram-бота с JSON-хранилищем (`bloggers/channels/offers/authSessions`), 4 режимами (`auto`, `auto_with_precheck`, `manual_approval`, `manual_posting`), webhook-интеграцией Telegram Bot API, инлайн-кнопками для решения по офферу (подтвердить/отклонить/выбрать другое время), ручным ERID-флоу, автопаузой на 24 часа, страницей рекламодателя для создания оффера по диапазону дат и выбору каналов, и админ-снэпшотом состояния. Реализуй state machine статусов оффера как в `cpvdemo-offer-state-machine.mmd`, тексты и кнопки как в текущем `cpvdemo/server.js`, test API (`/api/test/tick`, `/api/test/offers`) только для localhost при `ALLOW_TEST_API=true`, и UI-страницы в стиле текущего `cpvdemo/public/*`.
